/// Placeholder coordinator for AI-powered features (music generation, smart queuing, etc.).
/// This will eventually implement the QueueSource trait for AI integration.
#[derive(Debug, Default)]
pub struct AiCoordinator;

impl AiCoordinator {
    pub fn new() -> Self {
        Self
    }
}
